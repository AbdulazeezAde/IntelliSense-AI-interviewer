# Core runtime
fastapi>=0.95
uvicorn[standard]>=0.22
requests>=2.28
pydantic>=1.10

# Optional STT / reprocessing (install only if you need these features)
vosk>=0.3.45
faster-whisper>=0.4.0

# Storage & cache
azure-storage-blob>=12.18.0
redis>=4.5.0

# Testing / dev
pytest>=7.2
pytest-asyncio>=0.21
httpx>=0.24

# Dev helpers
python-dotenv>=0.21
